## Second brain (all agents — query first to save tokens)
This project is tracked in the second-brain vault at `~/Documents/graymatter/`.
- BEFORE reading this codebase cold, read `~/Documents/graymatter/Projects/Abbey Gold.md` and
  `~/Documents/graymatter/Brain/Workspace Map.md` — a note is a few hundred tokens; re-deriving from source is thousands.
- After meaningful work, log a session so the brain compounds: append to
  `~/Documents/graymatter/Dev Logs/<YYYY-MM-DD>.md`, and capture decisions to the project note.
- When the vault is wrong or thin, FIX the note — don't just answer from source. Write rules: `~/Documents/graymatter/_CLAUDE.md`.
